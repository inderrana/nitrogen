{
  "name": "n2-server",
  "version": "1.0.0",
  "description": "Encrypted Local Profile Manager",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "vercel-build": "echo 'No build step required'"
  },
  "dependencies": {
    "node-windows": "^1.0.0-beta.8"
  },
  "engines": {
    "node": ">=18.x"
  }
}
